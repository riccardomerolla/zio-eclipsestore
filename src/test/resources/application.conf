eclipsestore {
  storageTarget {
    fileSystem {
      path = "/tmp/zio-eclipsestore-config-test"
    }
  }
  maxParallelism = 8
  batchSize = 50
  backupDirectory = "/tmp/zio-eclipsestore-config-test/backup"
}
